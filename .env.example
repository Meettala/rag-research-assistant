# Optional. Without either key, the app runs in deterministic extractive-only
# mode with no external provider calls. Retrieval and answerability can still
# fail, so this mode reduces generation risk rather than claiming zero error.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
